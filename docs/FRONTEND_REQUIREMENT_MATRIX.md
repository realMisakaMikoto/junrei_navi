# Frontend redesign requirement and evidence matrix

Audit date: 2026-09-21. Scope: the complete approved September 15 frontend brief, including its Swift-inspired panel model, all existing pages, data contract, coordinates and acceptance gates. This is an evidence audit, not a completion declaration. The original plan-only restriction was superseded by the user's implementation authorization; personal-device installation and production changes remain separate.

### Explicit approvals on 2026-09-21

The user approved protected signing, the primary-web + both-provider-native + saved-coordinate replacement baseline and dedicated-emulator Google readiness checks without routes. Current signed `cb89265` now has API 37 AMap projection/pixel/theme/lifecycle passing execution evidence, with an initial same-byte click transient retained below. Original old/new same-ID equality remains unproven. API 26 native coverage still lacks a compatible ARM environment or an approved substitute; no answer, waiver or overall completion is assumed.

### Current signed snapshot and remaining device coverage

Verified application source and protected candidate are **`cb892650ad34512749760139ab8791df349ee541`**; ordinary CI **`35556295376`** and signed **`35557984317`** succeed. Later documentation-only commits do not change that application source. Three APKs have independent hash/fixed-signer checks; cleanup succeeded and SSH jobs skipped. Current signed API 26 x86 fallback, API 37 Google/bitmap/performance/night and the AMap three-case rerun pass at their exact hashes. Two separate single-case AMap reruns also pass. The initial full suite's click timeout remains recorded with unproven cause. Main remains `2b6d971`; API 26 native-coverage decision is still open.

### Final local addendum

The later `final-additional-evidence.json` manifests supersede historical pending auxiliary results: API 26 passed four startup-state cases, Google date-line/outlier FitAll and corrected native overlay (6 cases, 6.909 s); API 37 passed the same startup/FitAll cases (5 cases, 5.936 s). API 37's native-window flow recapture also passed four cases in 10.39 s. The API 26 overlay failure was stale accessibility child bounds after window movement; refreshing the resize node makes the unchanged exact size/origin/form checks pass. No production overlay code changed. The final reopened API 37 crop has no material visual finding. Native dialog capture and overlay synchronization are resolved test-harness issues, not open application defects.

The recorded UI/state baseline contains **51 distinct cases**: 14 discovery components, 14 existing UI contracts, 5 settings, 6 app-shell, 4 remaining flows, 5 startup/return-state and 3 transit presentation cases. Local passes and `3e4069a` CI establish this baseline. Its 7 Google native, 2 performance and 1 overlay checks give **61 grouped local checks per API**, with onboarding/TalkBack separate. These results belong to their recorded source and local debug target APK; they do not certify the new marker-font changes below.

The earlier local and `c8cac6e` CI passes remain scoped evidence. Its heap-failed signed attempt `35546582406` was superseded by successful `3f14597` run `35549085053`, validating the workflow memory correction. The later application repair has its own SIGNED-cb verification below.

### Marker typography correction and fresh local evidence

The baseline bitmap text ignored `fontScale` and used a 34 dp label canvas. The correction now uses actual Compose **14sp** conversion, measured glyph padding, minimum **48dp** bitmap dimensions and matching collision bounds; font changes invalidate marker presentation while retaining geographic anchors and image-card offsets. Bitmap dimensions do not claim undocumented SDK hit tolerance: actual off-dot label taps at normal/2x font are separately verified on Google.

The new production source passes **401 JVM cases**, debug/test assembly, both Lints and Release R8 in **6m 7s**, followed by fresh Google/AMap JNI/APK audits and all seven mutations. Both APIs pass two artwork cases and seven updated Google native cases; both normal/enlarged light/dark artwork sheets were reviewed without clipping. Corrected performance reruns pass two cases per API. The **53 UI/bitmap cases** also pass in exact-head ordinary CI at `c8cac6e`.

Two test-oracle issues were resolved without further production rendering changes: bitmap checks now compare an independent 14sp raster with matching Paint flags and every antialiased glyph pixel, replacing mismatched font-metric/exact-white measurements; performance ring sampling now uses measured radius + 1dp at actual density and independently counted cluster membership, replacing the obsolete fixed 20dp sample. All membership, interior/removal, image-reuse and timing/heap/frame assertions remain. Original failed runs are retained; final performance manifests supersede only their performance results.

**Overall acceptance remains incomplete.** Passing tests retain their recorded source/artifact scope. The approved signing and replacement coordinate basis have their scoped execution evidence; the API 26 native-coverage decision and final delivery record remain open.

## Status and evidence rules

- **V - implemented and verified at the stated scope:** the row names the passing evidence and its limits. This does not imply all-device or live-provider acceptance.
- **I - incomplete:** implementation may exist, but a required behavior, final-source rerun or meaningful acceptance observation is missing.
- **B - externally blocked:** the stated external prerequisite prevents the required check. No substitute or waived gate is assumed.
- When a grouped row contains a remaining gate, its overall status is I or B even if some subrequirements already pass.
- Synthetic data, Compose map placeholders, genuine SDK markers, authenticated base maps and physical navigation are different evidence classes. Activity relaunch is not process-death recovery. TalkBack accessibility actions do not establish audible speech or human gesture usability.
- `amapOverlapMarkerExposesItsCountToNativeAccessibility` checks SDK marker title/count metadata, not Android accessibility traversal or TalkBack marker focus. Actual TalkBack evidence remains the separate app-shell/list/selection service test; these scopes must not be conflated.

### Evidence inventory

| ID | Available evidence | Scope and limits |
| --- | --- | --- |
| J400 | Fresh `app/build/test-results/testDebugUnitTest/TEST-*.xml`: **400 tests, zero failures/errors/skips**, timestamps `2026-09-20T16:38:58Z` through `16:39:04Z`. | Latest confirmed combined local check. Includes discovery data/panel/search/map, shared selection and existing routing/persistence suites. Later fixes invalidate only their affected evidence; unchanged report files do not establish a new run. |
| BUILD-final | Coordinating task confirmed `testDebugUnitTest`, `assembleDebug`, `assembleDebugAndroidTest`, `lintDebug`, `lintRelease`, `minifyReleaseWithR8` succeeded together in **4m 24s**. | Fresh matching intermediates/mapping passed Google reflection audit, AMap JNI audit (7 classes/24 members), debug APK class checks, all 7 AMap audit mutations and APK content audit. This is local build/static-artifact evidence, not a keyed AMap render or the pending protected signed build. |
| FONT-build | New marker production source: **401 JVM, zero failures/errors/skips**, debug/test assembly, both Lints and R8 successful in **6m 7s**. | Fresh Google reflection, AMap 7-class/24-member/current debug APK, seven mutations and APK content audits pass against current outputs. New local debug target APK SHA-256 `4def1bc987dde4cc20d722b3448033e1d3309305a8132a1afd67582fe07bbb66`; older `4fc...` evidence remains historical. Corrected performance test assembly/Debug Lint additionally pass in 13 s. |
| FONT-native | `marker-font-api26/marker-font-evidence.json` and `marker-font-api37/marker-font-evidence.json`: artwork **2 passed in 0.572/1.154 s**; Google native **7 passed in 22.966/34.58 s**. | Target `4def...`, test SHA-256 `1fe59fe13068cc066efe491a37fa718914844cb9348590f64758ea114081951f`. Normal/2x label pixels/off-dot taps, stable anchors/camera/same View and one reused image pass. Both `marker-font-scaling.png` sheets are visually sound. The initial performance failures in these manifests are superseded by FONT-perf, not silently erased. |
| FONT-perf | `marker-font-api26-performance-final/marker-font-evidence.json` and API 37 equivalent: **2 passed per API, 19.582/29.695 s**. | Same `4def...` target, corrected test SHA-256 `d79c5ca461cc89f65af2a1e038932f375d74890aa7eb59ffc53b52f9ba2a1fa7`. Fresh metrics are under each final directory's `frontend-review`: exact 1k/10k/100k dense membership/pixels and nine viewport stages pass, with one image execution reused throughout. These are scoped Window/process observations, not continuous SDK FPS or a hard product performance budget. |
| U26 | Corrected full suite: **40 passed in 37.872 s**; current `evidence.json` captured `2026-09-20T16:55:13Z`. | Earlier two failures were confined to unsupported pre-API-28 Compose dialog capture. Native UiAutomation window capture fixes the test helper; no production change. All dialog flows now finish. App SHA-256 matches U37; test SHA-256 `21ac31f0671732c03cb2cef8c1585a6922194081466a48808aac8726d813e5e6`. |
| U37 | `build/frontend-review/api37/instrumentation.txt`: **40 passed in 66.117 s**; `evidence.json` captured `2026-09-20T16:44:57Z`. | Previous 36 contracts plus all four remaining flows. App SHA-256 `4fc9884d2f586fc3ea8b50bda34e4394088253abbadca3d1bcfad933e6b46e47`; test SHA-256 `1474e622350a2919cd97e333f6fea748b243fff6449dc0dfe60bced2054395af`. This precedes the test-only dialog-capture helper correction; API 26 rerun is tracked separately. |
| O26/O37 | Each API directory's `onboarding.txt`: one passed; permission, service-guide, completion and discovery relaunch markers. | Complete onboarding and fresh Activity launch on the dedicated emulators. No process-death or physical-device claim. |
| G37 | `api37/native-google-final.txt`: **4 passed in 13.139 s**; `native-evidence.json` records pass and `2026-09-20T16:46:20Z` execution. | Genuine Google view: projection/anchor/pixels/touch, drag with delayed metadata/image and padding, in-place browsing light/dark with immediate reduced-motion movement, and failed-image selectable-marker/request-cache regression. Synthetic content; no base-tile authorization, real guidance or sound claim. |
| G37-drag | `googleGestureSurvivesLateImageAndMeasuredPanelPadding` passed in G37; earlier two-case run was 9.523 s. | Metadata/image completion retain target and zoom after an actual drag; measured padding retains horizontal framing/zoom/bearing/tilt. No undocumented exact vertical-padding assertion. The latest four-case run supersedes the earlier pre-wrapper scope. |
| G37-theme-boundary | Corrected browsing-theme/reduced-motion case passed in G37; its earlier view-only night test lacked Navigator readiness. | That historical pass remains browsing-only. The separate later NAV-night evidence below now verifies actually enabled navigation UI; it does not retroactively change the old fixture's scope. |
| NAV-night | Both `native-navigation-theme-evidence.json` manifests and `frontend-review/native-navigation-theme-api26.json` / `api37.json`; combined consent + native theme runs: **API 26 5.799 s, API 37 9.535 s, two cases each**. | One case is explicit consent setup. Both JSONs record `navigatorReady=true`, `navigationUiEnabled=true`, `sameViewAcrossThemes=true`, fresh `nightModeEvents=[true,false]`, and no initial registration event. Explicit DARK/LIGHT events pass on SDK 7.8.0; no initial event is invented. No destinations/routes/guidance; permissions restored. Local debug target APK remains `4fc9884d...`; test APK SHA-256 `a674bbea50a57b7cd08cbf5c877b1c231bcb6c1212feaed6d5a48fee39181de0`. These additional authorized runs are separate from the earlier 61 grouped checks and ordinary CI. |
| CANDIDATE | `codex/frontend-signed-candidate` created at `3e4069a`; protection read back against fresh main: four checks bound to app 15368, strict, administrators, conversation resolution, no force/delete. Main/environment unchanged. | Signed run **`35541425419` failed terminally in setup-android**, whose pinned default requested obsolete `tools platform-tools`; signing materials were not restored and both SSH jobs skipped. Local fix explicitly requests `platform-tools`. It must pass normal CI at a new exact head before protected fast-forward and retry; no signed AMap artifact exists from this attempt. Detailed diagnosis is maintained once in the signed test plan. |
| CANDIDATE-retry | Historical candidate `c8cac6e`, after fresh protection/environment equality checks. | Signed run **`35546582406` failed with Java heap exhaustion** under 2GB; cleanup succeeded and SSH jobs skipped, with no artifact from that attempt. The later split/4GB correction is now actually validated by SIGNED-3f; this historical failure is no longer the current signing blocker. |
| SIGNED-3f | Protected candidate fast-forward/readback at `3f14597`; **signed run `35549085053` succeeded** with every guard/audit/cleanup and both SSH jobs skipped. | Independent `build/frontend-signed-35549085053/verified-artifacts.json` verifies all three hashes, fixed single signer/v2 and Release AMap JNI definitions. Exact hashes/bytes are tabulated in the signed test plan. This is the actual successful runtime validation of the memory correction, superseding the earlier failed attempt; these are internal candidates, not public v0.2.5 assets. |
| AMap-runtime | Signed debug/test packages installed only on dedicated AVDs, recreating their synthetic packages for signer transition. APK/process inspection confirms ARM-only AMap libraries versus selected x86_64 from the combined APK. | API 26 without a native bridge fails EGL context creation; API 37 initially has missing AMap libraries/black output. Same-byte API 37 `--abi arm64-v8a -r` preserves data and renders real tiles, but **three discovery cases still time out at `awaitPoint`**. No AMap projection/anchor pass. JDI obtained no useful camera fields and is not acceptance evidence. |
| SIGNED-night | `build/frontend-review/signed-35549085053-api37-arm64-night/`: Google consent + actual native night checks **2 passed in 12.287 s** on signed API 37 ARM. | One case is consent setup; same View and native `[true,false]` events, no routes/guidance, prior permissions restored. It verifies this signed snapshot only, not AMap discovery or later working-tree repairs. |
| API26-ARM | Official `anitabi-redesign-api26-arm64` created; emulator 37 rejects ARM and isolated official 34.2.16/31.3.14 stop before boot with HDA/no-PCI. Evidence: `build/frontend-review/api26-arm64-setup/`. | No running 5582 device or APK; old AVDs/global emulator preserved. User decision between a compatible dedicated ARM environment and an explicitly scoped API 37-native/API 26-UI-Google-static substitute is **pending**. No choice, waiver or API 26 AMap pass is assumed. |
| AMAP-repair | Full repaired local build **9m 57s**: 417 JVM with zero failures/skips, debug/test APKs, both Lints/R8; fresh Google/AMap 7-class/24-member/current debug APK and seven mutation audits pass. | Local build scope. Later exact-head CI, signed artifacts and runtime results are CI-repair, SIGNED-cb, ABI26-cb and the current API 37 entries below; ordinary UI/bitmap CI remains 53. |
| REPAIR-G26 | `build/frontend-review/amap-fix-google-api26/marker-font-evidence.json`: **2 artwork + 7 Google native (21.761 s) + 2 performance (21.896 s) pass**. | Repaired local debug target SHA-256 `e2eddfc43a447e09c7f9f429c25918e1e31e54f028e5e6b166c3b3bb916711b7`; test `9ea77782fb4bce0e7d1fe739c240390a10524bcde287f8ccaf8a4cc22974ded9`. Only API 26 synthetic packages changed back from fixed signer for this regression. API 37 remains on old signed ARM bytes; its prior results are not new-repair evidence. |
| V22 | Coordinating task's final review of **22 component/app PNGs** under the API 37 capture directory, followed by targeted normal-font half-panel control recapture. | The review found the remaining lower control overlap; all non-collapsed phone panels now place controls in the measured toolbar, and the targeted recapture passed. No other material finding remained within those reviewed synthetic scenes. This is a scoped visual pass, not evidence for actual maps, the new remaining-flow/overlay captures or every possible state combination. |
| P37 | `api37/native-performance-final.txt`: **2 passed in 22.53 s**; current dense and viewport metrics JSONs; `native-evidence.json` records `2026-09-20T16:46:47Z` execution. | Genuine Google view: 1k/10k/100k complete dense rebuild plus three viewport moves per unchanged data version. All nine viewport stages match exact visible membership/pixels and reuse one fixture image load. Timings, sampled process heaps and Window frames are reported below; no fixed product budget or continuous SDK FPS claim. |
| GP26 | `api26/native-google-performance-final.txt`: **6 passed in 29.076 s**; `api26/native-evidence.json` captured `2026-09-20T16:52:20Z`. | The original four native Google cases and two dense/viewport cases pass on API 26, bound to U37's app/test hashes. ADDITIONAL/REMAINING7 add three more distinct Google cases later. The unrelated Compose dialog helper failure is resolved; no AMap/navigation night claim follows. |
| FLOW-visual | Six new saved-record/editor/image screenshots reviewed, including the coordinating task's full-resolution check. | Close X and error/back toolbar are visible; no material defect was found in these six scenes. Earlier ambiguous reduced-size impressions were resolved by inspecting the full-resolution images. |
| OVL37 | Corrected `api37/native-overlay-final.txt`: **one passed in 5.081 s**; `overlay-evidence.json` captured `2026-09-20T16:55:19Z`, test SHA-256 matches U26's corrected helper artifact. | Initial reopen screenshot failure was a geometry/screenshot synchronization race; original containment/retained-frame assertions pass after synchronization, with no production fix. This manifest supersedes the historical failed native-evidence entry. Four palettes and final reopened crop are sound; API 26 also passes in ADDITIONAL. |
| ADDITIONAL | Each API's `final-additional-evidence.json`: API 26 6 passed in 6.909 s; API 37 5 passed in 5.936 s. | Both pass four original startup-state cases and Google date-line/outlier FitAll; API 26 also passes corrected overlay with original frame/form checks. API 37 four-flow native-window recapture separately passes in 10.39 s. Reopened overlay crop has no material finding; the earlier ongoing auxiliary statuses are superseded. |
| REMAINING7 | `remaining-ui-native-evidence.json` and `remaining-ui-native.txt` on both APIs: **7 passed**, API 26 17.057 s / API 37 22.93 s, captured `2026-09-20T17:28:54Z` / `17:29:00Z`. | Two discovery UI cases, three transit presentation cases and two actual Google cluster/minimal-focus cases. Both bind unchanged app SHA-256 `4fc9884d2f586fc3ea8b50bda34e4394088253abbadca3d1bcfad933e6b46e47` and test SHA-256 `ab5d117d29d707a83c26b04491f869f6ca29a1566a7c5ecf180d4e23a8d3fcd3`. UI fixtures, callbacks and native SDK projection/input scopes remain distinct. |
| RETURN-state | `return-state-evidence.json` on both APIs: **one passed**, API 26 0.315 s / API 37 0.326 s; captured `2026-09-20T17:35:35Z`. | Real DiscoveryViewModel waits for typed synthetic data, restores exact provider-tagged camera after detach, preserves it through completed local search, rejects detach/stale same-provider acknowledgement after a newer Focus, then accepts the matching acknowledgement. Test SHA-256 `8e6089943daa06d10a1dd77ce468258ff4a5be5e6f4b1c1e676075ef90ee9eff`; unchanged app hash. No SDK or GPS is involved in this one state case. |
| REMAINING-visual | Independent native-capture review of **exactly eight** new nearby/transit/external-control PNGs from REMAINING7. | No material issue found. These are synthetic component presentations, including short-height/large-font transit; they do not claim real transit schedules, GPS or external-app execution. |
| T37 | `api37/talkback.txt`: one passed in 5.164 s; `DiscoveryTalkBackInstrumentedTest`. | Actual enabled TalkBack service, Android accessibility focus/click, point selection and destination actions. Original service settings restored. API 26 TalkBack is not installed; speech and human gestures are unverified. |
| AMap-block | Historical local debug runs stopped at `AMAP_API_KEY_CONFIGURED=false`. | That earlier prerequisite is superseded by available signed packages; current unresolved ABI/environment and discovery-readiness results are AMap-runtime/API26-ARM. Neither historical failure nor actual tile rendering is a projection pass. |
| CI37 | PR #37 normal Android CI run `35520210411`, development head `b11ce4f`: all four required checks green, as recorded by the coordinating task's read-only check. | Normal CI, not the protected signed-candidate workflow. It does not bind later local panel/IME/motion edits or prove keyed AMap rendering. No raw CI logs were copied into this audit. |
| CI-current | Head `3f1459739a4f6dd196fb6e72f09f5e86c6aeb9e7`, normal CI **`35547547202`: all four checks successful**. | Independent artifacts `10617013808` / `10617312882` confirm **53 UI/bitmap cases per API**. These results and SIGNED-3f bind that exact snapshot; current AMap working-tree repairs require a new result. `c8cac6e` / `67e07a4` retain their own historical green CI evidence. |
| CI-repair | Exact repair `cb892650ad34512749760139ab8791df349ee541`, **`35556295376`: all four checks successful**. | Independently read artifacts `10620898633` / `10621063446` confirm **53 cases per API**. One local status read hit a TLS handshake timeout; a subsequent read succeeded, so this was not a CI failure. |
| SIGNED-cb | Exact `cb89265` signed **`35557984317` succeeded**, cleanup successful, SSH jobs skipped; independent `build/frontend-signed-35557984317/verified-artifacts.json`. | Fixed-signer/v2 APK hashes: Release `573db32f9743dff1471e9dbe1d33f3ea760cec2d86b705c48a01123906efb450`; debug `2dca03399f2c08f3377c9fa55487bc53cd7c198a92fe76f54df944cdb357c3a1`; test `3870416aad5a048db43ad76cc04db1c08d2a0f0e8a6a41d040a83b8cecc4939e`. First download failed before directory creation with cause unretained; retry of unchanged artifact verified successfully, without a confirmed network diagnosis. |
| AMap-cb | `signed-35557984317-api37-amap/signed-amap-evidence.json` preserves first-suite **2 pass/1 initial marker-click wait failure**; focused single-case then `...-amap-repeat/signed-amap-evidence.json` **3/3 pass** on unchanged installed hashes. | The initial failure was a five-second callback wait at `clickAnchor`/`verifyProvider`; no concrete production/fixture defect or root cause was established. Passing runs verify synthetic native projection/pixels/theme and provider lifecycle on API 37 ARM. Retain the transient reliability limitation without a new mandatory gate or a claim of a proven source fix. Marker-title/count evidence is not TalkBack traversal. |
| ABI26-cb | `signed-35557984317-api26-abi/signed-amap-abi-evidence.json`: **one passed**, zero failed/skipped, before/after installed hashes matched SIGNED-cb. | Verifies x86_64 unavailability before SDK View construction. It does not verify API 26 native AMap rendering/projection and does not resolve the pending environment/substitute choice. |
| Google37-cb | `signed-35557984317-api37-google/marker-font-evidence.json`: **artwork 2, Google native 7, performance 2 all pass** on SIGNED-cb hashes. | Exact new signed runtime evidence; no routes/guidance or transplantation of old target results. |
| Night37-cb | `signed-35557984317-api37-night/native-navigation-theme-evidence.json` and scalar theme JSON: **consent setup + night 2 pass**. | Navigator ready, navigation UI enabled, same View, events `[true,false]`, initial registration event absent; original fine/coarse permissions (both false) restored. No destinations/routes/guidance. |
| AMap-cb-visual | `signed-35557984317-api37-amap-visual/visual-evidence.json`: **one same-byte fixture pass**, installed hashes matched before/after. | Reviewed frame 07 light-dot and 08 dark-image markers have no material presentation issue. Frames 04/06 are loading and not acceptance evidence. Synthetic-grid captures do not newly prove authenticated geographic tiles; the earlier `3f14597` tile observation is separate. |
| CI-previous | Heads `3e4069a` / `67e07a4`, normal CI `35527164214` / `35543653061`: all four checks successful. | Historical exact-head evidence remains valid at its recorded scope; current source and signed-retry states are separate. |
| ROTATION | Each API's `rotation-coordinate-final.txt` and `rotation-coordinate-evidence.json`: one case passed, API 26 4.045 s / API 37 5.854 s. | Actual MainActivity portrait/landscape/portrait configuration and decor geometry, retained activity-owned SearchViewModel, exact selected IDs/coordinates and a late same-ID changed-coordinate input. Original orientation restored; no Room write, GPS or network. Saved Room equality remains separate JVM evidence. |
| CI-runtime | The green CI37 emulator jobs execute the mandatory `.github/workflows/android-ci.yml` recovery/runtime steps: `NavigationRuntimeInstrumentedTest#seedProcessRecoveryFixture`, force-stop and normal MainActivity launches, `#verifyFailedProcessRecoveryState`, `#foregroundServiceCompletesOfflineRouteAndPersistsProgress`, and `#foregroundServiceAutoArrivesFromMockGpsWhileScreenOff`. | Synthetic saved-route process restart/recovery UI, preserved point/order/progress on fail-closed route recovery, actual foreground service/manual completion, notification presence/removal, and mock-GPS screen-off progression/persistence. Workflow requires each pass marker. This is stronger than the local Activity-relaunch test; it still is not real GNSS, provider guidance, sound or physical-device evidence. |
| D | [Discovery data contract](DISCOVERY_DATA_CONTRACT.md). | Primary-source structural cross-checks and sanitized live aggregates; current web slots differ from pinned Swift slots. Live old/new point-coordinate equality has zero comparable pairs. |
| M | [Discovery map SDK contract](DISCOVERY_MAP_CONTRACT.md). | Official Google/AMap documentation consulted before implementation, projection/thread/padding/lifecycle/conversion constraints and source-level consequences. Documentation review is not device execution. |
| R | [Acceptance execution record](FRONTEND_REDESIGN_ACCEPTANCE.md), [signed test plan](FRONTEND_SIGNED_TEST_PLAN.md). | Historical checks, completed protected activation, failed first signing attempt and required new-head retry. Earlier successes do not automatically verify later source or signed artifacts. |

Ignored reports retain separate hashes: `4fc...`/`4def...` are historical local targets, REPAIR-G26 is local `e2ed...`, and current signed runtime is `2dca...`/`3870...`. SIGNED-cb and its current runtime manifests are available. Preserve the initial failed AMap manifest alongside successful unchanged-byte reruns; neither old artifacts nor successful retries erase that observation.

### Source path notation

Below, `main/` means `app/src/main/java/cn/anitabi/navigator/`, `test/` means `app/src/test/java/cn/anitabi/navigator/`, and `androidTest/` means `app/src/androidTest/java/cn/anitabi/navigator/`. Test class names identify files under those roots. No real titles, queries, point coordinates, route content, credentials or private endpoints are included.

### Reuse of unchanged navigation evidence

A source comparison with `2b6d971` found no redesign changes to `NavigationService.kt`, `NavigationViewModel.kt`, `NavigationRuntime.kt`, the core navigation engines, `ActiveTourEditor.kt`, `TransitOverlayLayout.kt` or handoff activity/launcher behavior. Their passing JVM/CI-runtime contracts remain valid at CI37's revision. The relevant new work is the app shell/saved-tour entry, themed navigation layout, overlay palette/text sizing and saved-route refresh publication. Preserve original requirements without turning a visual redesign into a new physical driving, spoken-output, external consumer-app or exhaustive state-machine acceptance campaign.

Navigation-related evidence and remaining scope:

1. **Changed UI entry/layout:** shell-home/trips, immediate arrival/stop and existing early-leave cases pass. REMAINING7 now also verifies populated transit timeline, refresh/error/action reachability and both external paused/resume/next callback paths, with eight reviewed scenes including nearby.
2. **New saved entry and edited dialog:** all four cases pass on both APIs, six scenes pass full-resolution review and API 37 native-window recapture passes. No new route engine or physical recovery test is required.
3. **Changed native overlay:** full tests pass on both APIs with original frame/form assertions, including corrected capture/node synchronization. Four palette scenes and the reopened crop are sound. Unchanged layout/action policies reuse J400 and notification lifetime reuses CI-runtime.
4. **Native theme wiring:** Google browsing/reduced-motion and actual Navigator-ready night/day transitions have recorded passes on both dedicated APIs. The current signed API 37 AMap presentation/theme fixture also passes. Consent setup is explicit and isolated; API 26 native AMap coverage remains Q02, without routes, guidance or physical driving/sound claims.

CI-runtime's process recovery can be cited through its exact existing selectors and required workflow pass markers. It need not be newly invented as an additional gate, and its synthetic/fail-closed scope stays explicit. Resolved capture/synchronization diagnoses do not imply production defects; local auxiliary reruns above are complete. Remaining provider/readiness and live-baseline gates stay explicit.

## 1. Product direction and navigation

| ID | Requirement from the approved brief | Implementation / evidence | Status and exact remaining evidence |
| --- | --- | --- | --- |
| N01 | Cross-work full discovery map; all valid members counted, density handled by aggregation; no Swift priority/outlier/member limit. | `main/ui/discovery/map/DiscoverySpatialIndex.kt`, `DiscoveryClusters.kt`; J400 `DiscoveryMapEngineTest` conservation, overlap, composite-ID and scale cases; P37 retains every fixture member. | **V (JVM + scoped Google workload).** No point-count cap is used; unresolved regions remain in the list and are withheld from SDKs. Broad device performance remains P01. |
| N02 | Keep Google/AMap regional split, Material 3/Android back, no MapKit/legacy providers or iOS glass; no comparison camera, cutouts, submissions, random works or cross-device relay. | `AppShell.kt`, `DiscoveryScreen.kt`, provider adapters, `Theme.kt`, `app/build.gradle.kts`; region/provider JVM tests and U26/U37 back/provider cases. | **V (source + synthetic contracts).** These remain scope boundaries, not new features to add. |
| N03 | Map/search/trips primary navigation, toolbar settings/about, separate fullscreen navigation task; context survives tab changes. | `main/ui/AppShell.kt`; `AppShellInstrumentedTest.destinationsPreserveQueryPointPanelAndSharedSelection`, `systemBackReturnsPointToSubjectThenOverviewWithoutLeavingHome`; U26/U37. | **V (synthetic MainActivity contracts at recorded snapshots).** Final-state rerun remains Q01. |
| N04 | Keep full first-use flow; ordinary startup location, then last viewport, then first-use Japan overview. | O26/O37, `DiscoveryStartupInstrumentedTest` on both APIs, default overview in `DiscoveryMap.kt`. | **V (real ViewModel, injected location and existing onboarding scope).** Denied/no-history, provider-tagged saved view, available/unavailable location and late response after manual movement pass. No real GPS-fix claim. |
| N05 | Restore active navigation at startup; returning home does not stop it; discovery camera never controls navigation follow. | AppShell/runtime recovery and lease code; recorded shell-home/trips cases, J400 and CI-runtime process/service checks. | **V (synthetic MainActivity, process-recovery and foreground-service scope).** Discovery never calls Navigator and the existing separate session remains intact. The actual cross-provider lease gate is C07; no current shell rerun or new physical drive/voice campaign is added. |

## 2. Single panel, homepage, selection and search

| ID | Requirement | Implementation / evidence | Status and exact remaining evidence |
| --- | --- | --- | --- |
| U01 | One discovery -> subject -> point content panel; type+ID detent/scroll memory; back restores previous layer; close detail and Android home back; independent fullscreen image task. | Panel/screen/viewer source; J400 and recorded component/app-shell panel/back tests. | **V (state and Compose behavior).** Current panel and fullscreen image close/error flows have recorded passes and reviewed captures under F14; no duplicate panel stack or pending original layout rerun is inferred. |
| U02 | Phone collapsed/half/expanded, home collapsed by default; content/font/available-space measured height with no unnecessary blank area; wide side panel reuses content and history. | `DiscoveryScreen.kt` actual toolbar/panel/header/footer/row measurements, non-filling lazy content, adaptive side width; U26/U37 36-case runs and V22. | **V (recorded component/app scope).** Measured-row/overview modes, short landscape, tablet and large-font cases pass; final reviewed panel/control findings are resolved by targeted recapture. Later relevant source-state checks remain Q01, not an unresolved original panel-design finding. |
| U03 | Floating search prompt; visible-work chips with cover/short label/count; selected filters pinned through pan; locate/reset/display controls. | Existing toolbar/control tests and V22; REMAINING7 `viewportCountsChangeWhileSelectedFilterStaysPinnedAndTripSelectionSurvives` on both APIs. | **V (component viewport-state scope).** Changing visible-member sets changes counts; a selected out-of-view work remains pinned with zero count before newly entering works, and trip selection survives filtering. SDK viewport/member publication is separately covered by P37/GP26; this is not a newly claimed whole-app native pan trace. |
| U04 | Collapsed viewport count/data state/selected summary; expanded points/works; nearby only with usable authorized location and explicit straight-line distance. | Existing summaries/works tests; N04 injected-location policy; REMAINING7 `nearbyRequiresLocationAndOrdersAllAvailablePointsByExplicitStraightLineDistance` and REMAINING-visual. | **V (state/component and reviewed nearby scope).** No location means no nearby choice, even after an unsuccessful locate action. An injected available location exposes all associated points in distance order with explicit straight-line labels; returning to viewport mode restores viewport membership and retains selection. No real GPS or city-boundary claim. |
| U05 | Normal map tap opens details; batch mode toggles selection; work filters never delete trip selection. | `DiscoveryScreen.kt` map callbacks; `DiscoveryViewModel.kt`; `SearchViewModel.kt`; J400 `SharedDiscoverySelectionTest`, U26/U37 independent-filter/provider case; G37 verifies native marker click dispatch. | **V (state + Compose + scoped Google click).** Full app native batch flow remains part of Q02. |
| U06 | Shared selection across map/search/planner; viewport select-all includes all clustered members; clear, multi-work planning, explicit provider switch and map-failure list fallback. | `DiscoveryRoute`, `SearchViewModel`, `AppShell`, `PilgrimageSelectionScreen`; J400 shared selection/provider/cluster tests; U26/U37 existing selection contracts and discovery list-slot disposal. | **V (JVM + synthetic UI contracts).** List fallback does not create the component map slot. Actual cross-provider SDK lease proof remains C07. |
| U07 | Local search after about 250 ms; subject/point/city groups; partial scope is honest; explicit Bangumi action/multi-selection; no implicit network search. | `DiscoveryViewModel` debounce, `DiscoverySearchIndex.kt`, `AppShell.localResults`, `SearchScreen.kt`; J400 11 search-index tests; U26/U37 now include `localSearchKeyboardActionDoesNotSendAnImplicitBangumiRequest`. | **V (JVM + both 36-case UI runs).** IME action leaves Bangumi request explicit; local category/completeness and selected-summary contracts pass. |
| U08 | City search uses source city association to frame points, no new geocoder or invented exact city boundary; map and search share state/index and return viewport. | J400 city association; existing real AppShell query/panel/tab contracts; RETURN-state real ViewModel detach/query/new-focus/acknowledgement case; separate Google adapter Focus/Restore/FitAll evidence in C02/C05. | **V (layered index, app-shell, state and Google adapter scope).** Exact provider-tagged camera survives detach and completed local search; newer Focus is not overwritten by detach/stale acknowledgement. This composition of evidence does not claim a new single end-to-end native Search-tab roundtrip. AMap adapter execution remains Q02. |
| U09 | Subject cover/summary and collapsible points; original/episode grouping shown only with metadata; missing metadata goes to ungrouped. | Subject/group source, D/J400, both APIs' grouping contract and V22. | **V (parser, synthetic UI and reviewed subject scene).** No point is dropped solely for missing group/episode. Q03 retains only overall provider/readiness acceptance. |
| U10 | Point screenshot/name/work/episode/timecode/description/source; fixed add/remove action; explicit secondary source link; retain planning/external flows. | Point/viewer/footer source, both APIs' detail/source-action contract, J400 and reviewed detail/flow scenes. | **V (mapping and synthetic actions/visuals).** Viewer failure/back behavior is verified in F14 and Google failed-image marker fallback in M02; no unfinished generic image workload remains. |

## 3. Complete marker representation and map computation

| ID | Requirement | Implementation / evidence | Status and exact remaining evidence |
| --- | --- | --- | --- |
| M01 | Far work-color dots/count clusters and occasional covers; medium reduced text; near labels and collision-avoiding screenshots; non-color selected outline/icon. | `DiscoveryClusters.kt`, `DiscoveryMarkerArtwork.kt`; J400 decoration/collision/selection cases; G37 dot/image/theme/selected pixels. | **V (JVM + scoped Google fixture).** Actual varied-density, two-provider visual coverage remains Q02/Q03. |
| M02 | Images on by default, switch off without removing members, missing/failed images fall back to ordinary points. | Settings/map/image code; J400 parser/member tests, U26/U37 missing-image selection, G37 image toggles/delay and `googleFailedImageRetainsSelectableMarkerWithoutRepeatedRequests`. | **V (source, JVM/UI and Google native fixture scope).** The failed-image case now passes with the marker still selectable and failed request cached. AMap rendering remains Q02. |
| M03 | Cluster click zooms; maximum zoom shows every overlap member; fit-all includes remote outliers, never truncates membership. | J400/member conservation; P37; ADDITIONAL FitAll; REMAINING7 `googleNativeClusterTapZoomsThenReturnsAllOverlapMembersAtSdkMaximum` on both APIs; shared overlap-panel source. | **V (Google native/input and shared member-list scope).** Actual cluster touch zooms, then at SDK maximum returns every overlap member; outlier/date-line FitAll separately passes. No point cap or priority pruning is introduced. AMap-specific native execution remains Q02/C07. |
| M04 | Versioned spatial index avoids repeated huge-object comparison; 300 ms idle debounce; data version + provider + sequence reject stale work. | `DiscoverySpatialIndex.kt`, `DiscoveryMapModel.kt`, `DiscoveryMap.kt`, `DiscoveryViewModel.prepareDiscoveryData`; J400 cancellation/generation/date-line tests. | **V (JVM + source).** Versioned worker publication, cancellation and provider tokens are covered; gesture-driven integration remains C04. |
| M05 | Index/query/cluster on workers; SDK projection/conversion/updates on Main; stable-ID add/update/remove, no replacement of unchanged native markers. | Map/adapters; J400 delta/metadata tests, G37 stable anchors and P37 `googleViewportUpdatesConserveMembershipAndReuseImages`. | **V (source/JVM delta and Google native viewport scope).** Nine viewport stages pass exact membership/pixels under unchanged data versions. Timing/memory/frame observations are recorded below. Native observations do not count internal SDK methods or directly prove object identity; stable-ID update behavior also relies on inspected source and JVM delta tests. |
| M06 | Only visible decorated images load; bounded image concurrency/cache/failures; pan/local search/filter/cluster never trigger bulk main-site fetch. | Map concurrency/cache/failure limits and repository/UI separation; D/M; G37 failed-image cache and P37 viewport-image reuse. | **V (source + synthetic native request scope).** One fixture ImageLoader execution is reused through all nine viewport stages, including image hidden/restored by padding. No public network was used; this is decoded-image reuse evidence, not a measurement of external HTTP traffic. Local search/filter remain disconnected from repository refresh by source contract. |

## 4. Main-site data, cache and architecture

| ID | Requirement | Implementation / evidence | Status and exact remaining evidence |
| --- | --- | --- | --- |
| D01 | Use only approved main-site index/dynamic-detail paths plus existing prioritized API; no Firebase/map key/cookie/mirror; image HTTPS host allowlist and versioned technical user agent. | `main/data/discovery/DiscoverySource.kt`, parser image validation; D; J400 `DiscoverySourceTest` request restrictions/redirect behavior and parser image tests. | **V (source, local HTTP tests and sanitized live primary-source research).** Discovery exception is documented in D; no new origins authorized. |
| D02 | Valid cached index first; draw once region/privacy ready; first connection or >24 h successful check refresh; manual refresh; index coordinates before details. | `DiscoveryRepository.kt`, `DiscoveryCache.kt`, `DiscoveryViewModel.kt`, `DiscoveryScreen.kt`; J400 fresh-cache/24-hour/manual-refresh tests. | **V (repository tests + source).** Real cold-start visual readiness remains N04/Q02. |
| D03 | Derive pages from index subject count/page size; one cache parameter per round, not used as data version. | `DiscoveryModels.kt`, `DiscoveryRepository.updateRound`, parser version digest; D; J400 `loadsDynamicPagesSeriallyWithOneTokenThenChecksIndexAgain`. | **V (live format cross-check + JVM).** No fixed seven-page assumption. |
| D04 | Serial throttled details; pause in background/resume; prioritize clicked incomplete subject; share duplicate in-flight task. | `DiscoveryRepository.kt` one network mutex, one-second spacing, worker queue and subject deferreds; J400 15 repository cases including cancellation, abandoned requests and follow-up refresh. | **V (synthetic concurrency/lifecycle tests + source).** No public request storm or credentials used by tests. |
| D05 | Verify current sample/current web parser/existing API/pinned Swift before translating compressed arrays; only typed domain/UI models outside adapter. | `DiscoveryParser.kt`, `DiscoveryModels.kt`; D primary-source evidence and field table; J400 `currentWebSlotsWinOverTheIncompatibleSwiftSlots`. | **V (format/mapping scope).** Current web slots 2/13 supersede incompatible Swift labels. Original old/new coordinate equality remains unproven; D12 records the verified, explicitly approved replacement basis. |
| D06 | Composite work::point identity; valid indexed coordinate required; directories excluded; duplicates/malformed/optional fields handled without silently dropping usable points. | `DiscoveryParser.kt`; J400 nine parser tests, spatial composite-ID tests, UI ungrouped preservation. | **V (JVM + source format).** Conflicting structural data fails closed; absent optional images/details leave indexed points. |
| D07 | Detail merge only enriches metadata; no coordinate override/new coordinate-less markers; selected/saved coordinates survive detail/index updates. | `DiscoveryParser.merge`, `SearchViewModel.kt`, `DiscoverySearchIndex.subjectSelection`; J400 parser/search/shared-selection cases, including index-backed Bangumi coordinates. | **V (synthetic exact-coordinate/selection tests).** Saved refresh equality/concurrency covered by F10; native rotation and cross-entry live baseline remain C06/D12. |
| D08 | Distinct index-available, details-loaded and current-generation states; missing page retry, explicit stale fallback, never mark old/partial data current. | `DiscoveryModels.kt`, `DiscoveryRepository.kt`, `DiscoveryScreen.discoveryDataLabel`, `AppShell.localResults`; J400 failed/missing/wrong-page, stale/search-completeness tests; U26/U37 partial/error copy. | **V (JVM + synthetic UI).** No completeness inferred from root API metadata alone. |
| D09 | Atomic valid-index switch; corrupt new data preserves old; independent public cache keeps current and previous generation only; no full-site image predownload. | `DiscoveryCache.kt`, `DiscoveryRepository.kt`; J400 four cache cases; D. | **V (file-cache JVM tests + source).** Pending write is transient; user Room and route responses remain separate. |
| D10 | Recheck index after pages with same round parameter; changed generation cannot declare detail completion. | `DiscoveryRepository.kt`; J400 `changedEndIndexCannotClaimConsistentGeneration`; D. | **V (JVM).** No stale generation promotion. |
| D11 | Add Navigation Compose and Material 3 Adaptive; Compose-native motion; split repository/ViewModel/shared selection/spatial calculation/provider adapter. | `app/build.gradle.kts` Navigation Compose 2.10.1 and Adaptive 1.3.0; `AppShell`, discovery data/UI/map files, `SearchViewModel`; J400/U26/U37. | **V (source + build/integration scope).** System reduced-motion behavior for existing native camera animation remains V04. |
| D12 | Original live old/new same-ID comparison; approved replacement is primary web contract + both-provider native projection + saved-coordinate regressions. | D, JVM/ROTATION, Google native and current signed AMap-cb projection passes provide the three approved evidence layers at their stated scopes. | **V (approved replacement scope).** Original old/new equality remains unproven with zero comparable pairs; it is never relabeled passed. API 37 AMap evidence does not waive the separate API 26 native-coverage decision; the initial click transient remains recorded. |

## 5. Every existing page and action

| ID | Required surface | Implementation / evidence | Status and exact remaining evidence |
| --- | --- | --- | --- |
| F01 | Discovery map, subject and point details: large map/single panel/filter-selection separation. | U01-U10/M01-M06, reviewed scenes, Google workloads and current signed AMap-cb projection/presentation/lifecycle evidence. | **I (API 26 native AMap coverage only).** Component/state and recorded API 37 provider scopes pass. Q02 retains the separate API 26 decision; no additional generic viewport workload is missing. |
| F02 | Batch/list: shared selection, viewport-all, clear, plan, provider switch and unavailable-map fallback. | `main/ui/search/SearchScreen.kt`, `PilgrimageMap.kt`, `AppShell.kt`; U26/U37 selection contracts; J400 provider/selection tests; V22. | **V (synthetic action contracts and reviewed list scene).** New native wrapper theme/motion validation is tracked separately in C08/V04. |
| F03 | Warm-paper search: local categories, explicit Bangumi and persistent selected summary. | `SearchScreen.kt`, `AppShell.localResults`, `Theme.kt`; J400 and U26/U37 36-case search/IME/short-height cases; V22. | **V (JVM, both UI runs and reviewed search scenes).** No outstanding IME-isolation or original search-layout finding remains. |
| F04 | Trips grouped into ongoing, current draft selection and saved tours. | Trips/repository code, J400, both APIs' active/draft and saved-entry/resume/replan/read-failure cases; V22/FLOW-visual. | **V (synthetic repository/UI and reviewed scenes).** Selected/active/saved entry and explicit actions preserve order/progress at the tested scope. The separate API 26 dialog-capture helper issue is resolved. |
| F05 | Planner paper form: mode/start/end/objective/stay/transit settings, generate action; external fallback only explicit; separate no-route details. | `main/ui/planner/PlannerScreen.kt`, planner ViewModel; U26/U37 planner mode, fallback and no-route contracts; J400 planner/transit suites; V22. | **V (JVM, synthetic actions and reviewed planner scene).** Final review need not expand into every possible page/state/device combination. |
| F06 | Preview map/summary, order edit, transit timeline, apply changes and start navigation. | Planner/preview code, U26/U37 contracts, J400 editor/planner tests, V22 and G37 browsing theme/reduced motion. | **V (existing logic, reviewed component scene and Google wrapper scope).** Corrected native browse-theme/motion check now passes. AMap wrapper evidence remains Q02 and future-editor new scene review F10; no new live route is required to reprove unchanged planning contracts. |
| F07 | Google road navigation keeps native instructions/voice; compact pilgrimage progress, confirm arrival and end. | Unchanged guidance/session source, U26/U37 controls, J400/CI-runtime, V22 and NAV-night on both APIs. | **V (preserved contracts and local native navigation-UI theme scope).** Actual Navigator-ready same-view DARK/LIGHT events pass; existing action/presentation evidence remains. No destination, route, guidance, physical driving or audible-voice result is claimed by the new test. |
| F08 | App transit emphasizes current leg, next action, stop/stay and refresh state. | Unchanged engines/J400/CI-runtime; REMAINING7 `TransitPresentationInstrumentedTest` current-leg case; REMAINING-visual. | **V (synthetic component presentation/actions on both APIs).** Populated second-leg timeline/stops/times, refresh-disabled state, recoverable error, retry availability and immediate actions pass at short height/large font. Reviewed scenes have no material issue; no real transit schedule/service claim. |
| F09 | Japan transit/AMap handoff clearly external; current leg, pause/resume/next/early-leave retained. | Existing early-leave case and unchanged handoff/J400 contracts; two REMAINING7 external-control cases; REMAINING-visual. | **V (component callbacks and retained baseline contracts).** Japan/light and AMap/dark fixtures expose explicit external execution and pass paused/resume/open/pause/next/stop callbacks. Existing early-leave test remains intact. No external app, route engine or service was launched by these presentation tests. |
| F10 | Future-stop editing and journey restore use shared components/errors while preserving state machine, order/settings/progress and persistence. | J400/CI-runtime, both APIs' remaining flows and FLOW-visual. | **V (synthetic repository/editor UI and reviewed scenes).** Fixed endpoint, terminal-state save failure/cancel and record preservation pass on both APIs. The API 26 test-helper capture failure is resolved; no physical recovery claim is added. |
| F11 | Floating controls and notifications follow theme; drag/resize/collapse/restore/immediate actions preserved. | J400/CI-runtime, OVL37 and API 26 final additional run. | **V (both native emulator overlays and baseline actions).** Drag/resize/theme/collapse/reopen pass with exact containment/frame/form assertions. The five API 37 crops have no material visual finding; original overlay permissions are restored. |
| F12 | Complete onboarding, step/permission explanation and bottom actions unified. | `OnboardingScreen.kt`, shared theme/readiness code; J400 readiness tests; O26/O37 full flow. | **V (dedicated-emulator functional flow).** Full steps and relaunch pass, with shared appearance/text rules in source. No physical permission-state changes are authorized or claimed. |
| F13 | About/settings group appearance/privacy/telemetry/sources/version; switches take effect immediately. | `AboutScreen.kt`, `AppSettingsStore.kt`, telemetry controller, `MainActivity.kt`; U26/U37 5 settings cases, telemetry immediate contract and shell appearance-relaunch test. | **V (settings/telemetry and synthetic app theme).** Actual AMap consent withdrawal/recreation and provider-wide image/appearance visuals remain C07/Q02. |
| F14 | On-demand fullscreen image viewer and distinct empty/partial/network/map/route/recovery errors. | J400, both APIs' error/image/open/back/failure/close cases, V22/FLOW-visual and ADDITIONAL recapture. | **V (both API flows and reviewed image/error scenes).** Close/back controls and the corrected API 26 capture/API 37 native-window recapture all pass. No generic image or recovery presentation run remains open. |

## 6. Visual system, accessibility, cameras and coordinates

| ID | Requirement | Implementation / evidence | Status and exact remaining evidence |
| --- | --- | --- | --- |
| V01 | Neutral map surfaces/floating controls/compact summary; warm-paper search/trips/about/onboarding, ink/vermilion/dividers; shared semantic components. | Shared source, reviewed scenes/overlays, Google night and current AMap-cb-visual evidence. | **V (reviewed presentation scopes).** Q03 records the actual API 37 AMap light/dark marker review. The separate API 26 native-provider coverage choice remains Q02. |
| V02 | System Chinese font; primary 16 sp, auxiliary 14 sp; >=48 dp touch targets; avoid unreadable large blur/transparency. | Existing controls plus FONT-build/FONT-native independent raster, dimension, clipping and normal/2x Google label-tap evidence. | **V (shared artwork and tested Google interaction scope).** Actual 14sp text conversion, minimum 48dp canvas and preserved anchors/View pass; native off-dot taps are separately tested. This does not assert an undocumented universal SDK hit rectangle. AMap execution remains Q02 and new-head CI remains Q01. |
| V03 | System/light/dark modes, separately authored warm dark layers, no automatic wallpaper palette; enlarged font/TalkBack. | Existing app-shell TalkBack and font evidence, current Google37-cb/Night37-cb and AMap-cb presentation/theme/marker metadata. | **V (recorded presentation and accessibility scopes).** Enlarged artwork and native theme/presentation checks pass. Marker metadata does not establish TalkBack marker traversal; API 26 native AMap coverage remains Q02. |
| V04 | Respect disabled animations. | AppShell/discovery immediate behavior; `MapCameraMotion.kt` checks Android animator setting for Google/AMap selection/preview; M official contract; BUILD-final and corrected G37 native case. | **V (source + Google runtime scope).** Disabled-motion camera movement is verified by the corrected passing test. AMap native key prerequisite remains Q02; no active-navigation night-mode claim follows from this motion result. |
| C01 | Source latitude then longitude, Double precision; Google WGS84 unchanged; AMap official conversion once; GCJ-02 route pass-through; storage/request/region checks use WGS84. | JVM precision/conversion/pass-through checks, current signed Google and AMap-cb projection/pixel evidence. | **V (recorded JVM and API 37 signed provider scope).** Actual AMap projection now has a passing unchanged-byte execution. No physical accuracy, API 26 native pass or proven explanation of the initial click transient is inferred. |
| C02 | Search/list focus at measured visible center; map tap minimal pan only when covered. | J400 focus math, real ViewModel command distinction, earlier G37 explicit-focus center, REMAINING7 `googleMinimalFocusKeepsVisibleTapStillAndOnlyUncoversHiddenPoint` on both APIs. | **V (Google native focus/minimal-pan scope).** Actual visible-point tap keeps camera still; a covered target receives the required limited pan. This complements the shared search/list command wiring and app-shell tests, without claiming a new full native Search-tab roundtrip. AMap remains Q02. |
| C03 | Detent changes update padding/control position without repeated whole-map fit; use actual panel/safe area, not modified coordinates or copied screen constants. | Prior panel/state evidence, REPAIR-G26, Google37-cb padding/gesture regression and AMap-cb measured centering/presentation. | **V (recorded shared-state and native fixture scopes).** Current signed API 37 runtime checks pass. No undocumented exact vertical-translation assertion or API 26 native AMap pass is inferred. |
| C04 | Manual map gesture cancels auto framing; late detail/location/panel updates do not reclaim viewport. | `DiscoveryViewModel.manualMove`/intent counters, map/adapters, J400 generation tests; G37-drag actually drags before delayed metadata/image and measured padding changes. | **V (Google native gesture/content/padding scope).** One-shot focus and source coordinates remain unchanged in the two-test pass. Late initial-location precedence remains bounded by N04 and AMap evidence by Q02; the Google manual-drag requirement is no longer untested. |
| C05 | Fit all covers full current subject/filter including outliers; cities use their associated points; navigation follow remains session-owned. | J400 selection/provider/city logic, N05 and native `googleFitAllIncludesDateLineMembersAndDistantOutlierInsidePadding` on both APIs. | **V (Google native framing plus shared selection logic).** A date-line subset and all members with a distant outlier fit measured padding, preserve source bits and pass outlier pixels/input. AMap-specific execution remains Q02/C07. Navigation ownership is the unchanged separate session path. |
| C06 | Provider projection drives viewport/cluster/hit; marker anchors stay geographic; detail/selection/theme/rotation/index refresh preserve saved points. | JVM saved-coordinate equality, separate ROTATION tests and current signed Google/AMap projection/pixel/touch/presentation runs. | **V (layered tested scope).** API 37 AMap anchor evidence now passes; this is not one native saved-tour end-to-end test or API 26 AMap proof. Preserve the initial click transient as a limitation, without inventing a new gate. |
| C07 | One active SDK view, old destroyed before switch; privacy before SDK; explicit provider switch and fail-closed region/single-provider trips. | Lease/privacy/UI contracts, current signed API 37 provider-switch/lifecycle/metadata passes and ABI26-cb safe fallback. | **V (API 37 native plus stated policy scopes).** Switch/lifecycle and marker metadata pass; metadata is not TalkBack traversal. API 26 fallback is not native rendering/projection, and its separate coverage decision remains open. |
| C08 | Standard maps without cloud styles/Map ID; browse light/dark, separate Google navigation night mode and AMap night map. | Current signed AMap presentation/theme pass, Google37-cb and Night37-cb scalar evidence. | **V (exact API 37 signed scope).** Native theme events and retained View pass without routes/guidance; absent initial registration event is recorded. Reviewed synthetic marker frames do not newly prove geographic tile authentication or API 26 AMap coverage. |
| C09 | Keep route responses memory-only, Room schema 2/StoredTourV2 compatibility, existing batching/shared quotas; no backend path/protocol/quota/deployment changes. | `StoredTourV2.kt`, `TourRepository.kt`, `main/data/local/AnitabiDatabase.kt`, route/batch suites; J400 schema/persistence/provider/batching/saved-refresh tests; D cache boundary. | **V (source + JVM regression scope).** No release, deployment or personal-device install is implied. Local artifact checks pass in Q04; the protected keyed candidate remains Q02 and the signed test plan. |

## 7. Performance, devices, release checks and delivery gates

| ID | Required gate | Available evidence | Status and exact next evidence |
| --- | --- | --- | --- |
| P01 | 1k/10k/100k synthetic calculation, incremental updates, memory and jank; never gain performance by hiding points. | FONT-build 401 JVM and fresh FONT-perf two cases on both APIs; exact dense/viewport membership/pixels, image reuse and metrics retained. | **V (new-source JVM + scoped Google workloads).** Both corrected performance reruns pass on target `4def...`; old `4fc...` metrics are historical. No hard budget, continuous-FPS certification or AMap performance pass is invented. |
| P02 | Date-line, cross-provider and invalid-region cases; group counts conserved; viewport-all covers members. | J400 spatial date-line/brute-force/cancellation/member conservation, region/classifier/provider resolver and shared-selection tests; U26/U37 provider/selection contracts. | **V (JVM + synthetic UI).** Native date-line framing and real provider switch remain C05/C07. |
| Q01 | Existing JVM/Compose semantics and new interaction regressions on dedicated API 26/37 emulators. | Exact `cb89265` all-green CI/53 per API, local repair checks, SIGNED-cb, ABI26-cb and current API 37 Google/AMap/night results. | **V (recorded source/artifact scopes).** New signed runtime executions are available; the original AMap transient remains recorded. Overall API 26 native-provider coverage is the separate unresolved Q02 decision. |
| Q02 | Real Google/AMap rendering, projection, anchors, lifecycle, native input and failure behavior. | Current signed API 37 AMap-cb, Google37-cb and Night37-cb passes; API 26 signed unsupported-ABI fallback passes. | **I (API 26 native-coverage decision only).** API 37 passing executions do not grant an API 26 waiver. Wait for the explicit compatible-environment/substitute choice. The isolated initial click timeout is an unproven transient limitation, not a newly required investigation gate or a claimed fixed defect. |
| Q03 | Representative device/layout/theme/font/TalkBack and final scoped visual review. | Existing reviewed scenes/overlays, font sheets and exact signed AMap-cb-visual fixture. | **V (reviewed presentation scopes).** Frames 07/08 show native light-dot/dark-image presentation without material issues; 04/06 loading frames are excluded. No fresh geographic-tile-authentication claim is made. Overall completion still depends on Q02. |
| Q04 | Debug/Release Lint, R8, provider audits/mutations, APK contents and fixed-signer checks. | Current local/ordinary CI and successful SIGNED-cb with independent three-APK hash/fixed-signer verification. | **V (exact signed artifact scope).** Run `35557984317`, cleanup and audits succeed; no public release or whole-product acceptance is inferred. |
| Q05 | Keep dedicated-emulator testing; physical install separately authorized, wireless ADB, preserve data, never connected instrumentation on personal device. | R/AGENTS constraints; all cited UI evidence is from named dedicated AVDs. | **V (execution boundary).** No physical install or drive/voice claim. If a later physical acceptance action is requested, verify device/package first and obtain its required authorization without clearing data. |
| Q06 | Safe repository synchronization preserving user changes; implementation order data -> shared state -> discovery -> existing pages -> whole acceptance; document main-site exception and actual results. | R records safe fast-forward from clean tracked baseline to `main@2b6d971`, development branch and scope; D documents exact origin exception; this matrix maps the complete brief. | **I (delivery).** Update coordinating project summary/acceptance with final results and remaining risks, preserve user attachments/changes, finish remaining provider/baseline gates before claiming completion. |

### Historical `4fc...` native dense and viewport metrics

The pre-font API 37 dense sample records publication-to-visible times of approximately **309 / 502 / 2,686 ms** and publication-to-pixels of **465 / 607 / 2,814 ms**. Its 100k maximum sampled Java/native heaps are **97,285,024 / 41,627,680 bytes**; Window reports are **2 / 1 / 2**, with **1 / 0 / 1 missed deadlines**. This section and the table below remain historical `4fc...` evidence and are not transplanted to the new APK.

The separate `api37/frontend-review/native-google-viewport-metrics-api37.json` covers three actual SDK viewport moves per unchanged data version: partial view, image inside excluded panel padding, then restored view. The fixture has eight overlap groups and one image point. All **nine stages** match exact expected visible membership and pixels:

| Dataset members | Visible members: partial / padding / restored | Camera-to-visible ms, rounded | Camera-to-pixels ms, rounded |
| --- | --- | --- | --- |
| 1,000 | 625 / 250 / 1,000 | 332 / 333 / 355 | 445 / 436 / 524 |
| 10,000 | 6,250 / 2,500 / 10,000 | 460 / 447 / 575 | 544 / 573 / 726 |
| 100,000 | 62,500 / 25,000 / 100,000 | 1,217 / 654 / 1,652 | 1,361 / 767 / 1,826 |

Every stage reports the same **one fixture ImageLoader execution before and after**, including hiding and restoring the image. This proves reuse of the unchanged decoded fixture image; the interceptor uses no network. It does not count public HTTP requests. The viewport workload samples at most **83,392,944 Java / 42,439,488 native bytes** across its stages. These are whole-process observations including fixtures/instrumentation, excluding GPU memory; they are not isolated map allocations or guaranteed process peaks.

Viewport Window reports total **48 samples**, with **14 missed deadlines** and maximum reported duration **34.76 ms**; each stage has only 4-6 reports. Timings include idle/projection/clustering, and pixel timing includes validation/screenshot polling. The test observes membership/pixels, not internal native marker method counts or identity. Together with source/JVM delta checks, it supplies bounded incremental-update evidence without hiding members to improve results.

The original brief asks for scale, incremental-update, memory and jank checks, not a numerical product budget or continuous-FPS certification. These measurements satisfy that scoped observation requirement and disclose their limits. The **120,000 ms** value in the JSON is only a test watchdog. Neither sparse Window reports nor a passing watchdog establishes universally smooth SDK surface rendering. GP26 independently passes the same six native Google/performance cases; the numbers above belong specifically to API 37.

## Remaining work, in dependency order

1. Await the explicit API 26 native-coverage choice: compatible dedicated ARM execution or the proposed scoped substitute. Do not assume a waiver or describe signed-x86 fallback as native AMap rendering.
2. Preserve exact SIGNED-cb artifact/runtime passes and the initial unchanged-byte click timeout. Its cause remains unproven; do not relabel it a fixed source bug or add an unsupported new gate. Original old/new coordinate equality remains unproven despite the approved replacement evidence layers now being available.
3. Record the user's final coverage decision and remaining boundaries before overall acceptance. No public release, deployment or personal-device installation is established by these results.

## Audit method and limitations

Read the full approved brief and current project constraints; inspected the relevant source, contracts, test declarations, local XML totals and sanitized emulator logs/metrics. A follow-up source comparison against `2b6d971` and review of CI workflow assertions identified the already-passing CI-runtime evidence and narrowed navigation gates to actual redesign changes. Did not inspect credentials, private attachments, real point/route payloads or raw CI logs. Did not run a build, contact GitHub, install an APK, change a device, change branch protection, or edit application code. Later results must be added with their own provenance rather than inferred from this snapshot.
