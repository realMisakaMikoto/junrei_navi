# 巡礼手帳 v0.2.5 候选隐私说明

最后更新：2026-08-09

v0.2.5 尚未公开发布。当前候选缺少经批准的生产地区数据、双地图书面授权和许可复核，因此中国路线与地图必须保持关闭；完整门禁见 [`docs/V0.2.5_RELEASE_GATE.md`](docs/V0.2.5_RELEASE_GATE.md)。公开稳定版 v0.2.4 的实际处理方式仍以其随包隐私说明为准。

## 本机保存的数据

应用只持久化用户拥有或明确选择的数据：首次导览状态、多作品选择、Anitabi 巡礼点、手动顺序、起终点、出行模式、停留设置、执行策略、提供方与地区版本标记、当前分段、暂停时间、已完成点和导航状态。用户点位与行程坐标始终以 WGS84 保存。应用禁止 Android 系统备份和设备迁移。

Google 或高德返回的路线矩阵、路线、GCJ-02 折线、步骤、预计时间和公交详情只保存在当前进程内存中，不写入 Room、SharedPreferences、文件或备份。升级时无法安全重新分类的旧行程会保留用户点位、顺序与进度，并明确要求刷新路线；应用不会静默清库，也不会把旧 Google 结果改标为高德结果。

## 地区判定与路线请求

规划或刷新路线前，Android 使用随候选提供的、经过许可和审核的版本化地区资产离线判定当前位置/明确起点与全部目的地。后端使用同版本资产独立复核。资产缺失、损坏、校验不符、版本不一致、点在边界、区域重叠、行程跨提供方或地区无法解析时，请求会在定位、计费和上游调用前失败。应用不以文字地址、时区、Geocoder、矩形或网络查询猜测地区。

规划请求只把完成请求所需的 WGS84 坐标、出行模式、优化目标、时间和公交偏好经 HTTPS 发送到项目自建 API；Android 不能指定地图提供方或坐标系。Android 使用 Firebase Anonymous Auth 获取匿名 ID Token，只用于验证项目路线服务访问，不需要邮箱、姓名或密码。

后端再次判定完整行程后，只选择一个提供方：中国大陆及经批准的中国官方地图专用区使用高德，香港、澳门、台湾、日本和其他地区使用 Google。Google 与高德内容不会在同一地图上混画，也不会在失败时自动回退到另一提供方。中国请求先由高德官方坐标转换服务把必要的 WGS84 点转换为 GCJ-02，再调用固定的高德 Web 服务；其他请求由服务器端 Google 服务账号调用固定的 Google Routes 上游。高德 Web Key、数字签名私钥和 Google 服务账号私钥都不会进入 APK。

服务器不缓存或持久化任何提供方的矩阵、路线、折线、步骤、公交正文或上游响应。SQLite 账本只按提供方、计费类别和 UTC 日/月周期保存项目共享的预留计数与计费开关，不以匿名 UID 设置个人额度。Firebase 匿名 UID 不作为额度或突发限速键；防滥用限速只在内存中保存服务器秘密生成的 HMAC-IP 摘要，并有空闲淘汰和容量上限。原始 IP 不写入账本或日志。应用与服务器日志不得包含 Token、原始 IP、坐标、动漫名、搜索词、请求正文、上游 URL、Key、签名或响应正文。

## Google、高德与 Firebase

非中国道路导航由设备上的 Google Navigation SDK 提供。Google SDK 可能依其适用条款处理设备位置和导航交互；项目无法替代 Google 的隐私政策。

中国行程的应用内地图使用高德 Android SDK。应用必须先展示本说明并取得当前隐私版本的单独同意，再调用高德隐私接口；在同意前、撤回后、高德 Android Key 缺失或生产地区数据未就绪时，不创建高德 MapView，也不调用其他高德 SDK 接口。高德 SDK 可能依其适用政策处理设备、网络、位置和地图交互信息；具体范围以发布时展示并经复核的高德隐私规则为准。用户可在“关于、隐私与数据来源”页面撤回高德 SDK 同意；撤回不会删除已保存的 WGS84 点位和行程进度，但会关闭应用内高德地图能力。

当用户明确开始中国行程的驾车、公交、步行或骑行分段时，应用可通过定向 Android URI 把该段 WGS84 起点、终点、名称和所选模式交给已安装的高德地图 App，并声明 `dev=1`。该操作只在用户点击后发生。应用不读取高德地图 App 的路线、班次、选择、导航或到达状态；若高德地图不可用或交接失败，应用只保留当前分段并提示重试，不打开 Google 或浏览器，也不伪造 ETA、班次或完成结果。

日本公共交通继续使用本机排序和逐段 Google 地图交接，不调用项目路线后端。交接只在用户明确点击后通过 Android `ACTION_VIEW` 发送一对 WGS84 端点和公共交通模式；应用不读取或推断 Google 地图的结果。日本与其他提供方点位不能混在同一行程中。

外部地图交接期间，应用使用 `location` 类型前台服务提供到达提示。首次交接要求 30 秒内且精度不差于 100 米的精确定位；接近判定只使用精度不差于 50 米的定位样本。用户可选择 Android 悬浮窗或可见前台通知作为控制入口；两者均不可用时不会打开外部地图。设备重启后只发布可恢复通知，不会自动启动定位、Activity、Google 地图或高德地图。

Firebase Analytics 与 Firebase Crashlytics 是两个独立、可撤回的可选开关，默认关闭。Analytics 只允许版本、设备能力、模式、点数区间、延迟区间和安全错误类型；Crashlytics 会在首次同意前删除未发送报告。两者都不得收集坐标、动漫名、搜索词、路线正文或用户 ID；撤回后停止收集并清理相应的本机待发送数据。

## Bangumi 与 Anitabi

动漫搜索直接访问 Bangumi API。巡礼点 JSON 和图片由 Android 以可识别的 `AnitabiNavigator/0.2.5` User-Agent、按用户操作低频访问 Anitabi 官方 API 与图片域名，不经过项目 VPS。应用不抓取 Anitabi 主站，也不打包或批量下载全站数据。

## 保留、故障与联系

自建 API 的 SQLite 共享额度账本与审计状态位于项目 VPS，并执行七日一致性备份。若恢复后无法证明额度没有回退，Google 与高德计费请求都保持关闭，直至人工对账。路线内容不进入备份。

断网、VPS 故障、隐私同意缺失、地区数据不确定或费用熔断时，已保存的 WGS84 行程和进度仍留在设备上，但路线无法刷新或对应地图不能启动。卸载应用会按 Android 行为删除其本机私有数据。

问题与安全报告方式见 [SECURITY.md](SECURITY.md)，项目联系人为 [realMisakaMikoto](https://github.com/realMisakaMikoto)。Google、高德与 Firebase 的数据处理还受各自适用条款和隐私政策约束。
